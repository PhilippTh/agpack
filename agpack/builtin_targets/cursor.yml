# Cursor (cursor.com)

skills:
  layout: directory
  path: .cursor/skills

commands:
  layout: file
  path: .cursor/commands

mcp:
  path: .cursor/mcp.json
  format: json
  servers_key: mcpServers
  transports:
    stdio: {}
    http: {}
    sse: {}
