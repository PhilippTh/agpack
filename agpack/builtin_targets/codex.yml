# OpenAI Codex CLI

skills:
  kind: copy-directory
  path: .codex/skills

agents:
  kind: copy-file
  path: .codex/agents

mcp:
  kind: edit-file
  path: .codex/config.toml
  merge:
    servers_key: mcp_servers
    transports:
      stdio: {}
      http:
        headers_key: http_headers
